{
	"name": "cannabot_test",
	"version": "0.1.0",
	"description": "testing the cannabot api and bot",
	"main": "dist",
	"types": "types",
	"scripts": {
		"build": "tsc",
		"clean": "rm -r -f dist && rm -r -f types",
		"rebuild": "npm run clean; npm run build",
		"start": "node test.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"axios": "^0.27.2",
		"node-ts-cache": "^4.4.0",
		"node-ts-cache-storage-memory": "^4.4.0"
	},
	"devDependencies": {
		"@types/node": "^18.7.18",
		"dotenv": "^16.0.2",
		"typescript": "^4.8.3"
	}
}
